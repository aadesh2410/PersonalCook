# Use legacy peer deps to handle React 19 compatibility with react-native-web
# react-native-web 0.19.13 requires React 18, but Expo 54 uses React 19
# This is safe for this project as React 19 is backward compatible
legacy-peer-deps=true
